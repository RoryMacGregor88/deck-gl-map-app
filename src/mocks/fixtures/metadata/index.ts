import { Metadata } from '~/types';

/** Dataset metadata */
import ListedBuildingsMetadata from './listed-buildings';

const metadataArray: Metadata[] = [ListedBuildingsMetadata];

export default metadataArray;
